@model CSM.Web.Models.CASAAccountDetailViewModel
@using CSM.Common.Resources
@using MvcPaging;
@helper LabelForSort(string headerText, string fieldName, string sortField, string sortOrder)
{
    <a href="javascript:;" onclick="sortField('@fieldName');return false;">
        @headerText
        @if (fieldName.ToLower().Equals(sortField.ToLower()))
        {
            if (sortOrder.Equals("ASC"))
            {<i class="fa fa-sort-asc"></i>}
            else
            {<i class="fa fa-sort-desc"></i> }
        }
        else
        {<i class="fa fa-sort"></i>}
    </a>
}

@if (Model.TransactionList == null || Model.TransactionList.Count() == 0)
{
    string sortField = Model.SearchFilter.SortField;
    string sortOrder = Model.SearchFilter.SortOrder;
    <div class="table-responsive margin-top-10">
        <table class="table table-hover datatable">
            <thead>
                <tr>
                    <th style="width:6%!important;">Customer Number</th>
                    <th style="width:10%!important;">Group Account Number</th>
                    <th style="width:10%!important;">Account No</th>
                    <th style="width:10%!important;">Procution Type</th>
                    <th style="width:10%!important;">Times Renew</th>
                    <th style="width:10%!important;">Product Term</th>
                    <th style="width:10%!important;">Effective Date</th>
                    <th style="width:7%!important;">Maturity Date</th>
                    <th style="width:7%!important;">Rate (%)</th>
                    <th style="width:10%!important;">Principal Amt</th>
                    <th style="width:10%!important;">Status</th>
                    <th style="width:10%!important;">Issuing Branch</th>
                    <th style="width:10%!important;">Passbook No./Original Cert</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td colspan="13" class="center">@Resource.Msg_NoRecords</td>
                </tr>
            </tbody>
        </table>
    </div>
}
else
{
    <div class="form-inline nopadding">
        <h5 class="form-title">Transaction</h5>
    </div>

    string sortField = Model.SearchFilter.SortField;
    string sortOrder = Model.SearchFilter.SortOrder;

    <div class="form-inline nopadding">
        <div class="form-group">
            @Html.Raw(Html.Pager(
                new Options
                {
                    PageSize = Model.SearchFilter.PageSize,
                    TotalItemCount = Model.SearchFilter.TotalRecords,
                    CurrentPage = Model.SearchFilter.PageNo,
                    ItemTexts = new ItemTexts
                    {
                        First = "<i class='fa fa-step-backward' style='font-size: 0.8em'></i>",
                        Previous = "<i class='fa fa-caret-left'></i>",
                        Next = "<i class='fa fa-caret-right'></i>",
                        Last = "<i class='fa fa-step-forward' style='font-size: 0.8em'></i>"
                    },
                    IsShowFirstLast = true,
                    CssClass = "pager",
                    IsShowInputPage = true
                },
                new { PageSize = ViewBag.PageSizeList }))
        </div>
        <div class="form-group">
            @Html.DropDownList("PageSize", new SelectList(ViewBag.PageSizeList, "Key", "Value"), new { @class = "form-control input-xs", id = "pagesizelist" })
        </div>
        <div class="form-group text-nowrap margin-left-8">
            <strong>
                @MvcHtmlString.Create(string.Format(Resource.Lbl_Paging, @Model.SearchFilter.FirstPageIndex, @Model.SearchFilter.LastPageIndex, @Model.SearchFilter.TotalRecordsDisplay))
            </strong>
        </div>
    </div>
    <div class="table-responsive">
        <table id="tblCustomerList" class="table table-hover datatable"  >
            <thead>
                <tr>
                    <th class="center text-nowrap" style="width:10%!important;">@LabelForSort("Txn Date", "TransactionDate", sortField, sortOrder)</th>
                    <th class="center text-nowrap" style="width:10%!important;">@LabelForSort("Value Date", "PostingTimeStamp", sortField, sortOrder)</th>
                    <th class="center text-nowrap" >@LabelForSort("รายละเอียด", "TransactionDescription", sortField, sortOrder)</th>
                    <th class="center text-nowrap" style="width:10%!important;">@LabelForSort("เลขที่เช็ค", "ChequeNumber", sortField, sortOrder)</th>
                    <th class="center text-nowrap" style="width:10%!important;">@LabelForSort("Dr/Cr", "DebitCreditCode", sortField, sortOrder)</th>
                    <th class="center text-nowrap" style="width:10%!important;">@LabelForSort("สาขา", "TransactionBranch", sortField, sortOrder)</th>
                    <th class="center text-nowrap" style="width:10%!important;">@LabelForSort("จำนวนเงิน", "TransactionAmount", sortField, sortOrder)</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.TransactionList)
                {
                    <tr>
                        <td class="center text-nowrap">@item.TransactionDateDisplay </td>
                        <td class="center text-nowrap">@item.PostingTimeStamp </td>
                        <td class="left text-nowrap">@item.TransactionDescription</td>
                        <td class="left text-nowrap">@item.ChequeNumber</td>
                        <td class="center text-nowrap">@item.DebitCreditCode</td>
                        <td class="left text-nowrap">@item.TransactionBranchDescription</td>
                        <td class="right text-nowrap">@item.TransactionAmount</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}